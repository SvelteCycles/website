<script type="text/javascript" src="//cdn.jsdelivr.net/jquery.slick/1.5.0/slick.min.js"></script>

<section class="title-wrap">
  <h2 class="page-name">Women | Jerseys</h2>
</section>

  <div class="slider multiple-items">
    <% if @womens_jerseys.any? %>
      <% @womens_jerseys.each do |jersey| %>
        <div class="item">
          <a href="/women/jerseys/<%= jersey.id %>">
            <img class="carousel-image" src="<%= jersey.product_images.first.photo.url %>"/>
          </a>
          <div class="item-text">
            <h4><%= jersey.product.name.upcase %></h4>
            <h4>£<%= jersey.product.price %></h4>
          </div>
        </div>
      <% end %>
      <% else %>
        <p>There are currently no jerseys available.</p>
    <% end %>
  </div>


<script>
  $(document).ready(function () {

      $('.multiple-items').slick({
          infinite: true,
          arrows: true,
          speed: 300,
          slidesToShow: 3,
          slidesToScroll: 2
      });


  });
</script>
