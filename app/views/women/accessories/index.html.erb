<script type="text/javascript" src="//cdn.jsdelivr.net/jquery.slick/1.5.0/slick.min.js"></script>

<section class="title-wrap">
  <h2 class="page-name">Women | Accessories</h2>
</section>

  <div class="slider multiple-items">
    <% if @womens_accessories.any? %>
      <% @womens_accessories.each do |accessory| %>
        <div class="item">
          <a href="/women/accessories/<%= accessory.id %>">
            <img class="carousel-image" src="<%= accessory.product_images.first.photo.url %>"/>
          </a>
          <div class="item-text">
            <h4><%= accessory.product.name.upcase %></h4>
            <h4>£<%= accessory.product.price %></h4>
          </div>
        </div>
      <% end %>
      <% else %>
        <p>There are currently no accessories available.</p>
    <% end %>
  </div>


<script>
  $(document).ready(function () {

      $('.multiple-items').slick({
          infinite: true,
          arrows: true,
          speed: 300,
          slidesToShow: 3,
          slidesToScroll: 2
      });


  });
</script>
